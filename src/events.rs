//==================================<===|===>===================================

//================================ InputEvent ==================================
pub enum InputEvent {

}


//================================= TickEvent ==================================
pub enum TickEvent {
    None,
    Done,
    //
    Syn,
}

impl TickEvent {
    pub fn is_done(&self) -> bool {
        if let TickEvent::Done = self {
            true
        } else {
            false
        }
    }
}
//==================================<===|===>===================================
