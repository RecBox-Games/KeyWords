const assetsSrc = ['ressources/keywords_backgound.png'];
const assets = [];
export const assetCount = () => assetsSrc.length;
const load_asset = (name) => {
};
export const load_assets = (res, rej) => {
    const arr = [];
    for (let asset of assetsSrc) {
    }
};
